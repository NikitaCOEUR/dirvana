# GoReleaser configuration
# Documentation: https://goreleaser.com

version: 2

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    main: ./cmd/dirvana
    binary: dirvana
    ldflags:
      - -s -w
      - -X github.com/NikitaCOEUR/dirvana/pkg/version.Version={{.Version}}
      - -X github.com/NikitaCOEUR/dirvana/pkg/version.GitCommit={{.ShortCommit}}
      - -X github.com/NikitaCOEUR/dirvana/pkg/version.BuildTime={{.Date}}

archives:
  - formats: [tar.gz]
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else }}{{ .Arch }}{{ end }}
    files:
      - README.md
      - LICENSE

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  use: github
  sort: asc
  abbrev: 7
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - '^ci:'
      - '^build:'
      - '^style:'
      - typo

release:
  github:
    owner: NikitaCOEUR
    name: dirvana
  draft: false
  prerelease: auto
  mode: replace
  name_template: "🧘 Dirvana {{ .Tag }} - Reach Directory Nirvana"
  header: |
    ## What's Changed
  footer: |
    **Full Changelog**: https://github.com/NikitaCOEUR/dirvana/compare/{{ .PreviousTag }}...{{ .Tag }}
